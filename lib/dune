(include_subdirs unqualified)

(library
 (name whilloc)
 (libraries z3 ppx_deriving_yojson.runtime encoding)
 (preprocess
  (pps ppx_deriving_yojson))
 (instrumentation
  (backend bisect_ppx)))

(ocamllex
 (modules lexer))

(menhir
 (modules parser)
 (flags --explain --dump))
