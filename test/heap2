function aenima() {    
    /*i = symbol("i");
    v = symbol("v");*/

    i = symbol_int("i");
    assume(i>=0);
    assume(i<8);
    v = symbol_int("v");
    /*v = 123; */
    x = new(8);

    x[i] = v;
    y = x[i];

    assert(y == v);

    delete x;
    return x
}